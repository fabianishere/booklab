// Test application setup
jwt {
    domain = "localhost"
    audience = "rest"
    realm = "booklab"
    passphrase = "abc 123"
    // The duration of the JWT token in milliseconds
    duration = 900000
}
